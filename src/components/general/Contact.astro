
<div class="py-10 px-4">
  <h2 class="text-4xl font-bold mb-8 text-center">
    <span class="relative inline-block">
      <span class="relative z-10 bg-gradient-to-r from-indigo-600 to-indigo-400 dark:from-indigo-500 dark:to-indigo-300 bg-clip-text text-transparent">Contacto</span>
      <span class="absolute left-0 bottom-1.5 w-full h-3 bg-yellow-300/30 dark:hidden -rotate-1 rounded shadow-sm"></span>
    </span>
  </h2>


  <div class="max-w-6xl mx-auto">
    <div class="md:hidden mb-6 px-4 py-6 bg-gradient-to-r from-indigo-50/80 to-transparent dark:from-indigo-900/20 dark:to-transparent rounded-lg">
      <div class="flex flex-col sm:flex-row items-center gap-4">
        <h3 class="text-3xl font-bold text-indigo-700 dark:text-indigo-400 animate-fade-in motion-reduce:animate-none text-center sm:text-left">
          ¿Hablamos?
        </h3>
      </div>
      <p class="text-gray-700 dark:text-gray-300 text-lg font-light leading-relaxed mt-2 animate-fade-in-delayed motion-reduce:animate-none text-center sm:text-left">
        Estoy disponible para nuevas oportunidades y colaboraciones.
      </p>
    </div>
    
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8 items-center">
      <div class="hidden md:flex flex-col justify-center p-6 lg:p-10">
        <h3 class="text-3xl  md:text-4xl font-bold text-indigo-700 dark:text-indigo-400 mb-6 animate-fade-in motion-reduce:animate-none">
          ¿Hablamos?
        </h3>
        <p class="text-gray-700 dark:text-gray-300 text-xl md:text-2xl font-light leading-relaxed opacity-0 animate-fade-in-delayed motion-reduce:animate-none">
          Estoy disponible para nuevas oportunidades y colaboraciones.
        </p>
        <p class="text-gray-700 dark:text-gray-300 text-lg md:text-xl mt-4 opacity-0 animate-fade-in-delayed-more border-l-4 border-indigo-500 pl-4 transition-all duration-300 hover:border-indigo-400 hover:translate-x-1 motion-reduce:animate-none">
          Envíame un mensaje y me pondré en contacto contigo lo antes posible.
        </p>
      </div>
      
      <form 
        id="contact-form"
        action= {`https://formspree.io/f/${import.meta.env.PUBLIC_FORMSPREE_FORM_ID}`}
        method="POST" 
        class="p-8 bg-white/95 dark:bg-gray-800/80 backdrop-blur-sm rounded-xl border border-gray-200 dark:border-gray-700/50 shadow-lg transition-all duration-300 md:col-start-2"
      >    
      <div class="space-y-6">
        <div>
          <label for="name-input" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Nombre</label>
          <input 
            type="text" 
            id="name-input" 
            name="name" 
            required
            class="w-full bg-gray-100 dark:bg-gray-700/70 border-0 border-b-2 border-indigo-200 dark:border-gray-600 text-gray-800 dark:text-white px-3 py-2.5 rounded-t-md focus:ring-0 focus:border-indigo-500 dark:focus:border-indigo-400 transition-colors"
            placeholder="Tu nombre"
          >
        </div>
      
      <div>
        <label for="email-input" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Email</label>
        <input 
          type="email" 
          id="email-input" 
          name="email" 
          required
          class="w-full bg-gray-100 dark:bg-gray-700/70 border-0 border-b-2 border-indigo-200 dark:border-gray-600 text-gray-800 dark:text-white px-3 py-2.5 rounded-t-md focus:ring-0 focus:border-indigo-500 dark:focus:border-indigo-400 transition-colors"
          placeholder="tu@email.com"
        >
      </div>
      
      <div>
        <label for="message-input" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Mensaje</label>
        <textarea 
          id="message-input" 
          name="message" 
          rows="4" 
          required
          class="w-full bg-gray-100 dark:bg-gray-700/70 border-0 border-b-2 border-indigo-200 dark:border-gray-600 text-gray-800 dark:text-white px-3 py-2.5 rounded-t-md focus:ring-0 focus:border-indigo-500 dark:focus:border-indigo-400 transition-colors resize-none"
          placeholder="Escribe tu mensaje aquí..."
        ></textarea>
      </div>
    </div>
    
    <div class="mt-6">
      <button 
        type="submit" 
        id="submit-button"
        class=" w-full py-3 px-4 bg-indigo-600 hover:bg-indigo-700 dark:bg-indigo-700 dark:hover:bg-indigo-600 text-white font-medium rounded-md transition-colors shadow-md"
        >
        Enviar
      </button>
    </div>
    <div id="form-status" class="min-h-6 mt-4 text-sm text-center"></div>
    <div class="flex justify-center mt-4 overflow-hidden w-full">
      <div class="transform scale-[0.78] sm:scale-[0.9] md:scale-100 origin-center">
        <div 
          class="g-recaptcha" 
          data-sitekey={import.meta.env.PUBLIC_RECAPTCHA_SITE_KEY}
          data-size="normal" 
          data-theme="light"
          data-callback="recaptchaCallback"></div>
      </div>
    </div>
    <p class="md:hidden text-center text-gray-500 dark:text-gray-400 text-sm text-wrap mt-4 italic  border-t border-gray-200 dark:border-gray-700 pt-4 opacity-0 animate-fade-in-delayed-more motion-reduce:animate-none">
      Me pondré en contacto contigo lo antes posible.
    </p>
  </form>
  </div>
</div>


<script is:inline>
  const form = document.getElementById("contact-form");
  const status = document.getElementById("form-status");
  const submitButton = document.getElementById('submit-button');
  let captchaOK = false;

  window.recaptchaCallback = function () {
    captchaOK = true;
    submitButton.disabled = false;
    submitButton.classList.remove('cursor-not-allowed', 'bg-indigo-400');
    submitButton.classList.add('bg-indigo-600', 'hover:bg-indigo-700');
    
    if (status.textContent.includes('captcha')) {
      status.textContent = '';
      status.classList.remove('text-red-500');
    }
  };

  window.recaptchaExpired = function () {
    captchaOK = false;
    submitButton.disabled = true;
    submitButton.classList.add('cursor-not-allowed');
    submitButton.classList.remove('hover:bg-indigo-700');
  };

  window.recaptchaError = function () {
    captchaOK = false;
    submitButton.disabled = true;
    status.classList.add("text-red-500");
    status.textContent = "Error con el captcha. Recarga la página.";
  };

  async function handleSubmit(event) {
    event.preventDefault();
    status.textContent = "";
    status.classList.remove("text-green-500", "text-red-500");

    if (!captchaOK) {
      status.classList.add("text-red-500");
      status.textContent = "Completa el captcha para continuar.";
      return;
    }

    const token = grecaptcha.getResponse();
    if (!token) {
      status.classList.add("text-red-500");
      status.textContent = "Error con el captcha. Recarga la página.";
      return;
    }

    submitButton.disabled = true;
    const originalLabel = submitButton.textContent;
    submitButton.textContent = "Enviando...";

    try {
      const data = new FormData(form);
      data.set('g-recaptcha-response', token);

      const resp = await fetch(form.action, {
        method: form.method,
        body: data,
        headers: { 'Accept': 'application/json' }
      });

      const payload = await resp.json().catch(() => ({}));

      if (resp.ok) {
        status.classList.add("text-green-500");
        status.textContent = "¡Mensaje enviado!";
        form.reset();
        grecaptcha.reset();
        captchaOK = false;
        submitButton.textContent = "¡Enviado!";
        
        setTimeout(() => {
          submitButton.textContent = originalLabel;
          submitButton.disabled = true;
          submitButton.classList.add('cursor-not-allowed');
          status.textContent = "";
        }, 2000);
      } else {
        submitButton.disabled = false;
        submitButton.textContent = originalLabel;
        status.classList.add("text-red-500");
        status.textContent = payload?.errors?.map(e => e.message || e).join(", ") || "Error al enviar. Intenta de nuevo.";
      }
    } catch (e) {
      submitButton.disabled = false;
      submitButton.textContent = "Enviar";
      status.classList.add("text-red-500");
      status.textContent = "Error de conexión. Intenta de nuevo.";
    }
  }

  form.addEventListener("submit", handleSubmit);
</script>
